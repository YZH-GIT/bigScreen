# bigScreen

Vue3 + TypeScript + Pinia 的大屏演示模板。

启动：

```bash
npm install
npm run dev
```

端口：默认 5174

集成 Tailwind CSS：

1. 已添加 Tailwind 配置与 PostCSS（`tailwind.config.cjs` / `postcss.config.cjs`）。
2. 入口样式文件：`src/styles.css`（已包含 Tailwind 指令）。
3. 本地启动前请运行 `npm install` 以安装新增依赖。
